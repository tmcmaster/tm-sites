{
    "scripts": {
        "sites": "cp sites.json ../docs",
        "web": "npx @pika/web --dest ../docs/web_modules",
        "build": "npm install && npm run web && npm run sites"
    },
    "dependencies": {
        "@wonkytech/template-web-component": "latest"
    }
}
